-- Las funciones MAX y MIN se usan para obtener el valor más alto o más bajo de una columna numérica

-- Consultar la edad máxima registrada en la tabla 'users'
SELECT MAX(age) FROM users;

-- Consultar la edad mínima registrada en la tabla 'users'
SELECT MIN(age) FROM users;
